<?php
/**
 * Persony Web Conferencing 2.0
 * @author      Persony, Inc. <info@persony.com>
 * @copyright   Copyright 2008 Persony, Inc.
 * @version     2.0
 * 
 */

/* unused
$memberId=GetSessionValue('member_id');

if (!isset($meeting) || !isset($meetingInfo['id'])) {
	ShowError("Meeting not found.");
} elseif  ($meetingInfo['host_id']!=$memberId) {
	ShowError("Not a host of the meeting.");
} else {
	if ($meeting->EndMeeting()!=ERR_NONE) {
		ShowError("Couldn't end the meeting. Error=".$meeting->GetErrorMsg());
	}
}

include_once("includes/mymeetings_list.php");
*/
?>